definitions:
  User:
    type: object
    properties:
      id:
        type: uuid
      username:
        type: string
      email:
        type: string
      password:
        type: string
    required:
      - username
      - email
      - password
